@page "/Clients/Form/{Id:int}"
@page "/Clients/Form"
@attribute [Authorize(Roles = Roles.AdminRole)]
@inherits BasePage

<PageTitle>@languageContainer.Keys[(Id == 0 ? "Add" : "Edit") + " Client"]</PageTitle>
<MudBreadcrumbs Items="breadcrumbItems"></MudBreadcrumbs>

@if (clientForm is null)
{
    <FormLoadingSpinner IsBigForm="true" />
}
else
{
    
}
